apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portal-webapp.fullname" . }}-test-connection"
  labels:
    {{- include "portal-webapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portal-webapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
