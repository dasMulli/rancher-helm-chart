apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portal-authenticator.fullname" . }}-test-connection"
  labels:
    {{- include "portal-authenticator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portal-authenticator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
